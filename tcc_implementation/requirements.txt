# Mantido como atalho de retrocompatibilidade.
# Estrutura nova em requirements/:
#   base.txt       — deps diretas (use em desenvolvimento)
#   base-lock.txt  — pip freeze do ambiente que rodou os experimentos
#   eval.txt       — avaliação oficial SemEval (ambiente separado)
#   dev.txt        — lint/format
#
# `pip install -r requirements.txt` resolve para o lock (reprodutível).
-r requirements/base-lock.txt